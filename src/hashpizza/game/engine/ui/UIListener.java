package hashpizza.game.engine.ui;

/**
 * Represents a class that is used to handle a UI event, e.g. a button click
 */
public interface UIListener {

    /**
     * Handles the UI event
     */
    void handleEvent();
}